{
  "name": "lifeos-self-reflection",
  "version": "0.1.0",
  "private": true,
  "description": "LifeOS Self Reflection — a check-in journal, an AI reading of what you wrote, and eleven life dimensions scored deterministically from your own check-ins.",
  "workspaces": [
    "packages/*",
    "app"
  ],
  "scripts": {
    "build": "npm run build -w @lifeos/core && npm run build -w @lifeos/self-reflection",
    "test": "npm run build -w @lifeos/core && npm run test -w @lifeos/core",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
